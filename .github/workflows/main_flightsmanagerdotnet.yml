# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy ASP.Net Core app to Azure Web App - FlightsManagerDOTNet

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code from the repository
      - uses: actions/checkout@v4

      # Set up .NET Core SDK
      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      # Restore dependencies
      - name: Restore dependencies
        run: dotnet restore dotnet-flights-mvc.sln  # Use the solution file

      # Build the project
      - name: Build with dotnet
        run: dotnet build dotnet-flights-mvc.sln --configuration Release  # Build the solution

      # Publish the project
      - name: dotnet publish
        run: dotnet publish dotnet-flights-mvc.sln --configuration Release --output ${{env.DOTNET_ROOT}}/myapp  # Publish the solution

      # Upload the artifact for the deployment job
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: ${{env.DOTNET_ROOT}}/myapp

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    permissions:
      id-token: write #This is required for requesting the JWT

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: .net-app
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_3E7DCD0A29DF4F588321C0DCF0A92C3F }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_FB956C7EBC3D42398FB5EC107F4B4733 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_FFC1EC64601E4A698CC201F58E7EC0C4 }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'FlightsManagerDOTNet'
          slot-name: 'Production'
          package: .
          